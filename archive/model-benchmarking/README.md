# Model Benchmarking Archive

Historical model-candidate benchmarking, manual live-smoke tooling, and generated proof evidence.

This is not part of the workshop runtime. It contains the former `server/model_eval`, `server/evals`, `server/test_support`, `server/evidence`, and gated/manual eval tests.
