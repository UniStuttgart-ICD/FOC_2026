# Historical Design Provenance

Superseded implementation plans, specs, and research notes retained for provenance.

These files are not workshop runtime guidance. Use `ARCHITECTURE.md`, `CONTEXT.md`, `docs/adr/`, and `server/runtime_profiles.toml` for current behavior.
